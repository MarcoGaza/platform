import reText from "./src/index.vue";
import { withInstall } from "@pureadmin/utils";

/** Support`Tooltip`Prompt text omission component */
export const ReText = withInstall(reText);

export default ReText;
